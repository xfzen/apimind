# Connector SDK release

The source module is `github.com/xfzen/ecp/sdk/go` and the first release tag is `sdk/go/v0.1.0`.

Local contract and import verification are implemented by `scripts/verify-release.sh`. Canonical tag publication, clean-module download through the official Go proxy, source commit, and module checksum remain intentionally unrecorded until the canonical `github.com/xfzen/ecp` repository is available and publication is explicitly authorized.

Release evidence fields:

- Tag: `sdk/go/v0.1.0` (pending publication)
- Source commit: pending
- Module checksum: pending
- Official proxy verification: pending
