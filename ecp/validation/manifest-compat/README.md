# Product Manifest compatibility prototype

The fixtures are JSON documents (and therefore valid YAML) so the prototype
needs no parser outside the frozen Node toolchain. It proves N/N-1 unknown-field
tolerance, capability intersection, stable error codes, canonical HMAC signing,
tamper rejection, and explicit downgrade refusal.
