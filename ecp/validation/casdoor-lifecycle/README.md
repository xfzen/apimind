# Casdoor identity lifecycle prototype

Validates stable external IDs, direct-group synchronization, disable behavior,
incremental list polling, deletion, reserved built-in namespace protection, and
continued owner-admin recovery access against the pinned Casdoor image.
